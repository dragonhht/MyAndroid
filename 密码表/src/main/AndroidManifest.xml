<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="dragon.hht.com.mina">

    <application android:allowBackup="true" android:icon="@mipmap/mima_ico"
        android:label="@string/app_name" android:supportsRtl="true" android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Pwd_Activity" ></activity>

        <activity android:name=".Regist_Activity" ></activity>

        <activity android:name=".Add_Activity" android:launchMode="singleTask"></activity>

        <activity android:name=".SelectDirectory_Activity" android:launchMode="singleTask"></activity>
        <activity android:name=".ExportData_Activity" android:launchMode="singleTask"></activity>

    </application>

    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

</manifest>